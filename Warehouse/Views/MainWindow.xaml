<Window x:Class="Warehouse.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Warehouse"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="MainWindow" Height="600" Width="700">
    <Grid Background="#ffdadada">
        <Border Background="White" CornerRadius="20" Margin="45">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Background="LightSlateGray" >
                    <Button x:Name="b1" Margin="10" Content="1234" Click="b1_Click"/>
                    <Button x:Name="b2" Margin="10" Content="1234" Click="b2_Click"/>
                    
                </StackPanel>
                <Frame x:Name="Framef" Grid.Column="1" Grid.ColumnSpan="2">
                    
                </Frame>
            </Grid>

           

        </Border>
    </Grid>
</Window>
